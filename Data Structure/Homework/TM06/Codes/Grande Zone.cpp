#include <bits/stdc++.h>
using namespace std;

int main(){
    deque<int> queue;
    int n;
    cin >> n;
    for(int i = 0; i < n; i++){
        int input;
        cin >> input;
        queue.push_back(input);
    }
    for(int i = 0;)
}